import 'package:flutter/material.dart';

class CampusIndex extends StatelessWidget {
  const CampusIndex({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Login Page')));
  }
}
