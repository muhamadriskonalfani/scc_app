import 'package:flutter/material.dart';

class DirectoryIndex extends StatelessWidget {
  const DirectoryIndex({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Login Page')));
  }
}
